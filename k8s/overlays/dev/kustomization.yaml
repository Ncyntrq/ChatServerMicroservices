apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - secret.yaml        # Secret dev-only (throwaway), không có trong base
# Dev: 1 replica (mặc định base), không HPA.
